#include <iostream>
#include <conio.h>
#include <stdio.h>
#include "cmd_process.h"
using std::cout;
using std::endl;
using std::cin;

void main(void)
{
	if(Initialize() == -1)
		return;
	
	while(true)
	{
		unsigned char ch = _getch();
		
		//This is for the arrow keys
		if( ch == 224 )
		{
			ch = _getch();
			if(ch == 72)      // Up arrow key
				MoveUpCursor();
			else if(ch == 80) // Down arrow key
				MoveDownCursor();
			else if(ch == 75) // Left arrow key
				MoveLeftCursor();
			else if(ch == 77) // Right arrow key
				MoveRightCursor();
		}
		
		//This is for the other keys which will be deleted later

		else if( ch == '[' )
            UpDownValue(-1);
        else if( ch == ']' )
            UpDownValue(1);
		else if( ch == '{')
			UpDownValue(-2);
		else if( ch == '}')
			UpDownValue(2);
		else if( ch == 27 )
		
		////////////////////ORIGINAL CODE UP
		
		
    int shelfHeight;
    int cabinetWidth;
    int cabinetHeight;
        cout << "Height of the midpoint of the upper left shelf (mm): " << endl;
        cin >> shelfHeight;
        cout << "Width of midpoint of upper left shelf to the midpoint of the next preceding shelf cabinet (mm): "<< endl;
        cin >> cabinetWidth;
        cout << "Height of a single cabinet (mm): " << endl;
        cin >> cabinetHeight;

        cout << " Your shelf height is: " << shelfHeight << endl;
        cout << " Your cabinet width is: " << cabinetWidth << endl;
        cout << " Your cabinet height is: " << cabinetHeight << endl;

  return 0;

//


		//obtain value from database
		if (the camera sees a shelf, delay the time a bit then start these if statements){

		

		}



		{
			Terminate();
			break;
		}

	}

}
